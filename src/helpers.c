#include "helpers.h"
#include "debug.h"

/* Helper function definitions go here */

unsigned int get_total_block_size(unsigned int requested_memory_size)
{
}

void get_payload_size(unsigned int request_size, unsigned int* total_size, unsigned int* padding_size)
{

}

// set block(int allocated, block size, request size)
    // block can be 1st block ever
    // block can be middle block
    // block can be last block